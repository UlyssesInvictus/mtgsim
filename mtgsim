#!/usr/bin/env bash
# MTG Mana Base Simulator wrapper script

# Get the directory where this script is located
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Execute the Python script with all arguments forwarded
python3 "${SCRIPT_DIR}/mtg_sim.py" "$@"
